SENDGRID_KEY=
MAIL_FROM=noreply@amer247.com

# Mettpay (pay-online checkout) — create-order endpoint is public-facing per
# Mettpay's own convention, but the merchant credentials are server-only.
NEXT_PUBLIC_API_URL=
MERCHANT_KEY=
MERCHANT_SECRET=
