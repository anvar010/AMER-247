# Dependencies
node_modules/
**/node_modules/

# Next.js build output
.next/
**/.next/
out/
**/out/
dist/
**/dist/

# Environment files
.env
.env.*

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# IDE
.vscode/
.idea/

# Claude Code skills & internal tooling — not part of the project
.agents/
.claude/
.mcp.json
skills-lock.json

# Local Supabase schema reference — not needed at runtime, and shouldn't be
# pushed to other remotes this repo gets mirrored to.
supabase-setup.sql

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Raw uncompressed hero video source (89MB) — the site serves the compressed
# AMER-hero.mp4; keep the original out of git history.
public/images/AMER.mp4

# Raw uncompressed splash hero video source (128MB) — over GitHub's 100MB
# push limit anyway. The site serves the compressed public/images/FNL-web.mp4
# and the extracted public/hero-bg-fr/ frames; keep the original out of git.
public/images/FNL.mp4

# Backup of the previous hero-bg-fr frame set, kept locally only — not
# referenced by any code.
public/hero-bg-fr-2/
