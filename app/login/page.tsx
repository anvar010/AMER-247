import MobileLoginScreen from "@/components/MobileLoginScreen/MobileLoginScreen";

export const metadata = {
  title: "Sign In — Amer 24/7",
};

export default function LoginPage() {
  return <MobileLoginScreen />;
}
